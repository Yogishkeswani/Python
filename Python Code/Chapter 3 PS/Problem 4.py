# Replace the double space from problem 3 with single spaces.

name = "Harry  is a good boy and"

print(name) # name will be pinted as it is

print(name.replace("  "," ")) # we used a pre defined Function and replaced double spaces with a single space (as said in the question)

